//progam in c ,calculate the area of circle using the #define preprocessor and getch() function.
#define pi 3.14
int main(){
    float area,radius=4;
    area=radius*radius*pi;
    printf("%f",area);
    getch();
}
